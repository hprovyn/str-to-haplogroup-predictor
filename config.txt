pythonPath=#python directory
htslibPath=#htslib directory containing tabix and bgzip
pathSeparator=#windows '\', unix '/'
pythonScriptsDir=#directory where you put the python scripts
experimentDir=#directory to put all files necessary to run the experiment
predictionDir=#directory to put all files necessary to make a prediction
resultsFile=#csv file containing STR and SNP results where each line contains a single ID, MARKER, ALLELE
treeFile=#YFull tree in json format
utilityUnderSpecificityError=0
utilityOverSpecificityError=0
utilityCompletelyWrongError=0
utilityCorrect=0
percentMissingSTRThreshold=20
cladeFinderMaxThreads=20
createCSVinputMaxThreads=25
